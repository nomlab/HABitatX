<p style="color: green"><%= notice %></p>

<%= render @item %>

<div>
  <%= link_to "Edit", edit_item_path(@item), class: "button" %> |
  <%= link_to "Back", items_path, class: "button" %>

  <%= button_to "Destroy", @item, method: :delete, class: "button" %>
</div>
